rules_version = '2';

// Permissive rules for Test Mode verification
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}