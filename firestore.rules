
rules_version = '2';

// Orvyra Startup Network Security Rules
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Application pitches: anyone can apply, only admins (auth users) can review
    match /applications/{applicationId} {
      allow create: if true;
      allow read: if request.auth != null;
    }

    // Founder profiles: public read, auth write
    match /profiles/{profileId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Test collection: anyone can write for verification purposes
    match /test_collection/{testId} {
      allow create, update: if true;
      allow read: if true;
    }
    
    // Default deny for everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
